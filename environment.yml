name: somatix
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.12
  - numpy
  - pandas
  - h5py
  - pysam
  - scikit-learn
  - matplotlib
  - progressbar33
  - pip
  - pytorch
  - htslib

# Note: PyTorch is omitted from this file so you can install a GPU-enabled
# build that matches your system CUDA runtime. See `conda_install_commands.sh`
# and `README.md` for PyTorch install examples.